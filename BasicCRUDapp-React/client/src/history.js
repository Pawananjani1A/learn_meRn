// We have to import browserHistory beforehand ans export default it to make it available
//throughout the project
import { createBrowserHistory } from 'history';


export default createBrowserHistory();